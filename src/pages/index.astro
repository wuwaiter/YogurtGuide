---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
import { formatBatchCultureLabel, formatBatchDate } from '../lib/batch-display';

const batches = (await getCollection('batches', ({ data }) => !data.draft))
	.sort((a, b) => b.data.date.localeCompare(a.data.date) || b.id.localeCompare(a.id))
	.slice(0, 1);
const base = import.meta.env.BASE_URL;
---

<BaseLayout>
	<section class="hero">
		<div class="hero__inner">
			<img
				class="hero__logo"
				src={`${base}brand/logo-block.png`}
				alt="YogurtGuide"
				width="960"
				height="540"
			/>
			<h1 class="hero__title">優格製程筆記</h1>
			<p class="hero__lead">
				以優格種類、菌種（species / strain）與製法（process）為軸，記錄可對照、可重複的發酵實驗。
			</p>
			<p class="hero__social">
				<a href="https://www.instagram.com/yogurtguide" target="_blank" rel="noopener noreferrer">Instagram</a>
				<a href="https://www.threads.com/@yogurtguide" target="_blank" rel="noopener noreferrer">Threads</a>
			</p>
		</div>
	</section>

	<section class="section">
		<div class="wrap">
			<div class="section__head">
				<h2>最新批次（Latest Batch）</h2>
				<p>每次實作固定記錄日期溫度、食材與菌種、成品狀況。</p>
			</div>
			<ul class="list">
				{
					batches.map(({ id, data }) => (
						<li>
							<a class="list-item" href={`${base}batches/${id}/`}>
								<h3>{formatBatchDate(data.date)}</h3>
								<p class="meta">
									{formatBatchCultureLabel(data)} · {data.resultSet}
								</p>
							</a>
						</li>
					))
				}
			</ul>
		</div>
	</section>
</BaseLayout>
